{
  "name": "studroute-max",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "СтудМаршрут — MAX-бот и мини-приложение для иностранных студентов СПбГЭУ",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "BOT_MODE=polling ALLOW_DEMO=true node src/server.js",
    "test": "node --test",
    "check:bot": "node scripts/get-me.js",
    "webhook:set": "node scripts/setup-webhook.js"
  }
}
